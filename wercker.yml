box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.18"
        theme: beautifulhugo
        flags: --buildDrafts=false
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - uetchy/gh-pages:
        token: $GIT_TOKEN
        repo: esaezgil/esaezgil.github.io
        path: public
        domain: https://esaezgil.com
